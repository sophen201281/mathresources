121
const button121 = document.getElementById('button121');

let index121 = 0;

const colors121 = [ 'red','green'];

button121.addEventListener('click', function onClick() {
  button121.style.backgroundColor = colors121[index121];
  button121.style.color = 'blue';

  index121 = index121 >= colors121.length - 1 ? 0 : index121 + 1;
});
122
const button122 = document.getElementById('button122');

let index122 = 0;

const colors122 = [ 'red','green'];

button122.addEventListener('click', function onClick() {
  button122.style.backgroundColor = colors122[index122];
  button122.style.color = 'Blue';

  index122 = index122 >= colors122.length - 1 ? 0 : index122 + 1;
});
123
const button123 = document.getElementById('button123');

let index123 = 0;

const colors123 = [ 'red','green'];

button123.addEventListener('click', function onClick() {
  button123.style.backgroundColor = colors123[index123];
  button123.style.color = 'blue';

  index123 = index123 >= colors123.length - 1 ? 0 : index123 + 1;
});
124
const button124 = document.getElementById('button124');

let index124 = 0;

const colors124 = [ 'red','green'];

button124.addEventListener('click', function onClick() {
  button124.style.backgroundColor = colors124[index124];
  button124.style.color = 'Blue';

  index124 = index124 >= colors124.length - 1 ? 0 : index124 + 1;
});
125
const button125 = document.getElementById('button125');

let index125 = 0;

const colors125 = [ 'red','green'];

button125.addEventListener('click', function onClick() {
  button125.style.backgroundColor = colors125[index125];
  button125.style.color = 'Blue';

  index125 = index125 >= colors125.length - 1 ? 0 : index125 + 1;
});
126
const button126 = document.getElementById('button126');

let index126 = 0;

const colors126 = [ 'red','green'];

button126.addEventListener('click', function onClick() {
  button126.style.backgroundColor = colors126[index126];
  button126.style.color = 'Blue';

  index126 = index126 >= colors126.length - 1 ? 0 : index126 + 1;
});

127
const button127 = document.getElementById('button127');

let index127 = 0;

const colors127 = [ 'red','green'];

button127.addEventListener('click', function onClick() {
  button127.style.backgroundColor = colors127[index127];
  button127.style.color = 'Blue';

  index127 = index127 >= colors127.length - 1 ? 0 : index127 + 1;
});

128
const button128 = document.getElementById('button128');

let index128 = 0;

const colors128 = [ 'red','green'];

button128.addEventListener('click', function onClick() {
  button128.style.backgroundColor = colors128[index128];
  button128.style.color = 'Blue';

  index128 = index128 >= colors128.length - 1 ? 0 : index128 + 1;
});

129
const button129 = document.getElementById('button129');

let index129 = 0;

const colors129 = [ 'red','green'];

button129.addEventListener('click', function onClick() {
  button129.style.backgroundColor = colors129[index129];
  button129.style.color = 'Blue';

  index129 = index129 >= colors129.length - 1 ? 0 : index129 + 1;
});
130
const button130 = document.getElementById('button130');

let index130 = 0;

const colors130 = [ 'red','green'];

button130.addEventListener('click', function onClick() {
  button130.style.backgroundColor = colors130[index130];
  button130.style.color = 'Blue';

  index130 = index130 >= colors130.length - 1 ? 0 : index130 + 1;
});

131
const button131 = document.getElementById('button131');

let index131 = 0;

const colors131 = [ 'red','green'];

button131.addEventListener('click', function onClick() {
  button131.style.backgroundColor = colors131[index131];
  button131.style.color = 'Blue';

  index131 = index131 >= colors131.length - 1 ? 0 : index131 + 1;
});

132
const button132 = document.getElementById('button132');

let index132 = 0;

const colors132 = [ 'red','green'];

button132.addEventListener('click', function onClick() {
  button132.style.backgroundColor = colors132[index132];
  button132.style.color = 'Blue';

  index132 = index132 >= colors132.length - 1 ? 0 : index132 + 1;
});

133
const button133 = document.getElementById('button133');

let index133 = 0;

const colors133 = [ 'red','green'];

button133.addEventListener('click', function onClick() {
  button133.style.backgroundColor = colors133[index133];
  button133.style.color = 'Blue';

  index133 = index133 >= colors133.length - 1 ? 0 : index133 + 1;
});

134
const button13 = document.getElementById('button134');

let index134 = 0;

const colors13 = [ 'red','green'];

button134.addEventListener('click', function onClick() {
  button134.style.backgroundColor = colors134[index134];
  button134.style.color = 'Blue';

  index134 = index134 >= colors134.length - 1 ? 0 : index134 + 1;
});

135
const button135 = document.getElementById('button135');

let index135 = 0;

const colors135 = [ 'red','green'];

button135.addEventListener('click', function onClick() {
  button135.style.backgroundColor = colors135[index135];
  button135.style.color = 'Blue';

  index135 = index135 >= colors135.length - 1 ? 0 : index135 + 1;
});

136
const button136 = document.getElementById('button136');

let index136 = 0;

const colors136 = [ 'red','green'];

button136.addEventListener('click', function onClick() {
  button136.style.backgroundColor = colors136[index136];
  button136.style.color = 'Blue';

  index136 = index136 >= colors136.length - 1 ? 0 : index136 + 1;
});

137
const button137 = document.getElementById('button137');

let index137 = 0;

const colors137 = [ 'red','green'];

button137.addEventListener('click', function onClick() {
  button137.style.backgroundColor = colors137[index137];
  button137.style.color = 'Blue';

  index137 = index137 >= colors137.length - 1 ? 0 : index137 + 1;
});

138
const button138 = document.getElementById('button138');

let index138 = 0;

const colors138 = [ 'red','green'];

button138.addEventListener('click', function onClick() {
  button138.style.backgroundColor = colors138[index138];
  button138.style.color = 'Blue';

  index138 = index138 >= colors138.length - 1 ? 0 : index138 + 1;
});

139
const button139 = document.getElementById('button139');

let index139 = 0;

const colors139 = [ 'red','green'];

button139.addEventListener('click', function onClick() {
  button139.style.backgroundColor = colors139[index139];
  button139.style.color = 'Blue';

  index139 = index139 >= colors139.length - 1 ? 0 : index139 + 1;
});

140
const button140 = document.getElementById('button140');

let index140 = 0;

const colors140 = [ 'red','green'];

button140.addEventListener('click', function onClick() {
  button140.style.backgroundColor = colors140[index140];
  button140.style.color = 'Blue';

  index140 = index140 >= colors140.length - 1 ? 0 : index140 + 1;
});

